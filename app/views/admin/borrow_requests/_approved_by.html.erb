<%= turbo_frame_tag "approved_by_#{borrow_request.id}" do %>
  <input type="text" readonly class="form-control"
        value="<%= borrow_request.approved_by_admin_name || t(".not_approved") %>">
<% end %>
