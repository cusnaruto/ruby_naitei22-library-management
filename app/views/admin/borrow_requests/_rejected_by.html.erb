<%= turbo_frame_tag "rejected_by_#{borrow_request.id}" do %>
  <input type="text" readonly class="form-control"
         value="<%= borrow_request.rejected_by_admin_name || t(".not_rejected") %>">
<% end %>
